#include "monty.h"
/**
 * func_stack - prints the top
 * @head: stack head
 * @line_num: the line of the opcode being executed
 * Return: no return
*/

void func_stack(stack_t **head, unsigned int line_num)
{
	(void)head;
	(void)line_num;
	bus.ln = 0;
}
