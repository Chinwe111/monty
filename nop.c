#include "monty.h"
/**
  *func_nop - nothing
  *stack: a pointer to the stack
  *@line_number: the working line number of a Monty bytecode file
  *Return: no return
 */

void func_nop(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;
}
